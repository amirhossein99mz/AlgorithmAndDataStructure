# Common Substring

This program finds the length of the longest common substring between two strings.

Example:

str1 = 123ABCD34EFG
str2 = XXXABCE124YABCD

Compile:

gcc common_substring.c -o app

Run:

./app

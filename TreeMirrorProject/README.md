# Tree Mirror Variant

This program duplicates each node in a binary tree and attaches the copy either to the left or right side.

## Concept
Traverse the tree recursively and insert a duplicate node next to each original node.

## Build
Use CLion or CMake.

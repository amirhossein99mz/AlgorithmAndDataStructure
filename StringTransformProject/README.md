# String Transform

This program processes a string by reversing only strictly increasing segments of characters.

## Idea
Scan the string, detect ascending runs, and reverse each run independently.

## Build
Open with CLion or use CMake.

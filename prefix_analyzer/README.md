# Prefix Analyzer

This program determines the shared starting characters among a group of strings.

## Idea
Compare characters column-wise across all strings until a mismatch appears.

## Build
Open with CLion or use CMake.

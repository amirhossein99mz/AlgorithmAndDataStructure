# Diagonal Difference Program

## Description

This C program finds the matrix element whose diagonal and inverse-diagonal contain the maximum difference between the largest and smallest values.

The program:
- checks every matrix element
- scans diagonal directions
- computes max - min
- prints the indices of the best element

## Compile

```bash
gcc max_diff.c -o app
```

## Run

```bash
./app
```

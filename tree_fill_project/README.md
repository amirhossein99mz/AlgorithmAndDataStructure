# Binary Tree Fill

Complete binary tree insertion using BFS queue.

Function:
tree_t *fill(tree_t *root, char *s);

Behavior:
Insert new string node into first available left-to-right position.

Complexity:
Time: O(n)
Space: O(n)

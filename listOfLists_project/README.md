# listOfLists

This project implements a Binary Search Tree (BST) where each node stores:
- an integer key
- a doubly linked list of strings

If multiple rows in the file have the same integer value, the strings are stored inside the same linked list of the BST node.

Main function:
- insert(char *name)

The function reads the file and creates the BST structure.

# Matrix Path Explorer

This program explores paths in a grid and evaluates them based on accumulated values.

## Idea
It uses recursive traversal (DFS) to explore all possible movements and tracks the best result.

## Build
Open with CLion or use CMake.
